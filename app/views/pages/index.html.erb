<% if current_user %>
  <div class="row">
    <div class="page-header">
      <h1>What are you waiting for?</h1>
      <div class="col-md-12 col-md-offset-3 instructions">
        <p>Navigate to accounts in the top-right, then select the view employees drop-down.</p>
      </div>
    </div>
  </div>

<% else %>
    <%= render 'welcome' %>
<% end %>